"""Codex → AI Gateway compatibility proxy.

Run with ``python3 -m proxy.server``. See ``server.py`` for the HTTP relay,
``rewrite.py`` for request-side rewrites, and ``stream.py`` for response-side
rewrites.
"""
